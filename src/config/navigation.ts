export const storeNavigation = [
  {
    label: "Products",
    href: "/products",
  },
  {
    label: "Categories",
    href: "/categories",
  },
] as const;

// ? as const:
// Treat them as readonly literal values
